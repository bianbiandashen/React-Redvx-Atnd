/**
 * Created by Administrator on 2016/11/23.
 */
const prefix = 't_';

const entities = {
    token: `${prefix}token`,
    user: `${prefix}user`,
    house: `${prefix}house`,
    room: `${prefix}room`,
    devices: `${prefix}devices`,
    gateway: `${prefix}gateway`,
    frequentlyDevice: `${prefix}frequentlyDevice`,
    message: `${prefix}message`,
    scenes: `${prefix}scenes`
};

module.exports = entities;
